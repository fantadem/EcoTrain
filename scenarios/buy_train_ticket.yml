# To be used with GreenIT Analysis CLI
- name: Acheter un billet de train
  url: http://localhost/
  waitForSelector: button
  actions:
    - name : Sélection de la gare de départ
      type : select
      element : select[name="departureStation"]
      values: ['Paris']

    - name : Sélection de la gare d'arrivée
      type : select
      element : select[name="arrivalStation"]
      values: ['Troyes']

    - name: Choisir la date
      type : type
      element: input[name="date"]
      text: "2025-01-01"

    - name: Choisir l'horaire
      type: select
      element: select[name="departureTime"]
      values: ['08:00']

    - name: Lancer la recherche de trajet
      type: click
      element: button[type="submit"]
      pageChange: true
      waitForSelector: article

    - name: Consulter les trajets disponibles
      type: scroll
      count: 3

    - name: Sélectionner le trajet de son choix
      type: click
      element: article:nth-child(3) button
      pageChange: true
      waitForSelector: .trip-details
      
    - name: Consulter les détails d'un trajet
      type: click
      element: .trip-details button
      pageChange: true
      waitForSelector: .pay-button

    - name: Consulter son panier et accéder au paiement
      type: click
      element: .pay-button button
      pageChange: false
